# WS3 — Release operations (v0.4.0 Epics 1-3) — post-rebase status

**Branch**: `feat/v0.4.0-release-ops` at `cf5900d`, rebased onto `main` (`f8ffc77`) on 2026-05-14. \
**Wave**: WS3 ships on top of WS1 (v0.3.0-5 cloud completion) + WS2 (v0.3.0-6 Kubernetes). \
**Ready to merge**: `git merge --ff-only feat/v0.4.0-release-ops` from `~/source/repos/signalman`.

## Rebase outcome

- Original WS3 history: 12 commits ahead of the old `558e0ed` base.
- Post-rebase: **11 commits** ahead of `f8ffc77`. One commit (`2b5c0de`, "cross-link approver identity to signalman-cloud contract") was dropped during rebase because its entire diff was a `.workstream-status.md` edit, and WS1's status doc owns that filename in `main`. The technical content of `2b5c0de` (the OSS↔Cloud contract cross-link) is already shipped in `signalman-cloud:b2f3930` (pushed to its origin) so nothing is lost.
- Conflict shape was exactly as the WS6 handoff predicted: WS1's cloud-repo classes + WS3's scheduled-health / webhook / promotion classes co-exist at the same insertion points in `driver.ts`, `index.ts`, `sqlite.ts`, `postgres.ts`, `cli.ts`, `server.ts`, `verbs/control-plane.ts`. Resolution strategy: take main's clean version of each file (via `git checkout --ours`), then append WS3 additions in the order originally committed. Where the auto-merge succeeded on later commits, the in-order append produced clean joins. Where it didn't, the file-level scoped append landed cleanly because all WS3 additions are end-of-file or new-section.
- `.workstream-status.md` add/add conflicts on every WS3 commit that touched the file were resolved by taking main's version (WS1 owns that file post-merge). This standalone `.workstream-status-ws3.md` is the WS3 post-rebase status.

## Commits on top of main (11)

| # | Hash | Subject |
|---|---|---|
| 1 | `ef79a0f` | feat(v0.4.0-3): scheduled health-check schema + scheduler |
| 2 | `252f4cc` | feat(v0.4.0-3): scheduled-health CLI + MCP surface |
| 3 | `b8fc52b` | feat(v0.4.0-3): signalman-schedule-health skill |
| 4 | `77a2b17` | feat(v0.4.0-2): webhook schema + dispatcher + HMAC signing |
| 5 | `8ff0467` | feat(v0.4.0-2): Slack + email drivers + verb-layer event firing |
| 6 | `2fbac51` | feat(v0.4.0-2): webhook CLI + MCP + skill |
| 7 | `1201e81` | feat(v0.4.0-1): promotion schema + listener |
| 8 | `0111c29` | feat(v0.4.0-1): promotion CLI + MCP |
| 9 | `99184fb` | feat(v0.4.0-1): signalman-promote-release skill + status |
| 10 | `78a8e33` | feat(v0.4.0-1): tier-to-tier listener + approver allow-list |
| 11 | `cf5900d` | docs(v0.4.0-1): keep OSS honour-system text deployment-agnostic |

## Verification (Definition of Done gates)

| Gate | Result |
|---|---|
| `cd host && npx tsc --noEmit` | **clean** — zero errors |
| `cd host && npm test` | **2305 passed / 2 skipped / 0 failed** across 124 test files |
| `cd host && npm run coverage --testTimeout=30000` | **83.68% lines / 82.04% branches / 87.47% functions / 83.68% statements** — all four above the 80/70/80/80 threshold |
| Working tree clean | yes (except the consumed `WS3-REBASE-PROMPT.md` which gets removed before merge) |

## WS3 deliverables — unchanged from pre-rebase

WS3 shipped three v0.4.0 epics intact through the rebase:

- **Epic 3 — Scheduled health checks (v0.4.0-3)**: `health_schedule` table + scheduler module (pure `dueSchedules` + `runSchedulerTick` + `startScheduler`) + 7 CLI subcommands + 6 MCP tools + `signalman-schedule-health` skill. 27 tests (unit + integration + system w/ fake clock).
- **Epic 2 — Webhooks (v0.4.0-2)**: `webhook_subscription` table + `EventDispatcher` + HMAC signer + Slack Block-Kit formatter + nodemailer-backed email (gated on `SIGNALMAN_SMTP_URL`) + 4 CLI subcommands + 4 MCP tools + `signalman-add-webhook` skill. Fires `release-built` / `release-deployed` / `deployment-rolled-back` from `runReleaseBuild` / `Deploy` / `Rollback`. 39 tests (unit + integration + E2E against in-memory `http.Server`).
- **Epic 1 — Auto-promotion + approval gates (v0.4.0-1)**: `promotion_policy` + `approval` tables + listener with `onReleaseBuilt`, `onReleaseDeployed` (tier-to-tier), `firePolicy`, `runPromotionTick` + 7 CLI subcommands + 7 MCP tools + `signalman-promote-release` skill. Per-policy honour-system approver allow-list (`gate_config.approvers`). `release show` surfaces attached approvals; approve/reject emit `promotion-approved` / `promotion-rejected` webhook events. 44 tests.

Per-epic 4-lens audits (QA / Architecture / Product / Security — all PASS) remain valid through the rebase since no code changed during conflict resolution beyond the file-level positioning of WS3 additions. The audit content lived in `.workstream-status.md` pre-rebase; that file is now owned by WS1, so the audit narratives are preserved in the WS3 commit messages themselves and in this branch's git log.

## Cross-stream observations from the rebase

1. **No WS3 hunk was reshaped or dropped silently.** The skipped commit (`2b5c0de`) was a pure-status-doc edit whose technical content (cross-link to `signalman-cloud:docs/contracts/promotion-approvers.md`) is already shipped on the Cloud side. Every code-bearing WS3 commit landed with its full diff.

2. **WS3 × WS2 `runner deploy-k8s` integration question** — answered with no follow-up commit needed. WS2's `runK8sDeployVerb` carries an explicit doc comment: *"Direct K8s deploy — bypasses the control-plane Deployment row and runs `runK8sDeploy` on an explicit bundle/namespace. ... the control-plane release-deploy verb stays the audit-logged path."* My tier-to-tier promotion listener fires from `runReleaseDeploy` when the resulting `Deployment` row reaches `status === "active"`. Because WS2's k8s path deliberately doesn't write Deployment rows, k8s deploys deliberately don't trigger downstream promotion today. If a future WS extends `runK8sDeployVerb` to write Deployment rows + fire `release-deployed` events, my listener picks them up automatically — the seam is already in place.

3. **WS3 × WS1 per-org credentials integration question** — no integration needed. WS1's credentials path covers cloud-provider auth (AWS/Azure creds at rest); WS3's approver allow-list covers human identity for promotion decisions. They're orthogonal concerns. If a future WS wants "Cloud RBAC over OSS honour-system approvers", the contract spec is in `signalman-cloud:docs/contracts/promotion-approvers.md`.

## Operator review needed before merge

- **`.workstream-status.md` dropped my Epic 1-3 audit narrative.** WS1's status doc now owns the filename. If the operator wants a consolidated post-merge audit doc that includes the WS3 4-lens audits, the source material lives in:
  - Pre-rebase WS3 status: original tip `31a153b` (now reflog-only; `git show 31a153b:.workstream-status.md` resurrects it).
  - This file (`.workstream-status-ws3.md`) summarizes WS3 deliverables but doesn't re-print the full per-epic 4-lens text.
  - Commit messages on commits 1, 4, 7 (the per-epic primary commits) carry the substantive audit notes.
- **The promotion `release-deployed` listener's tier-to-tier semantics** are documented in the skill (`signalman-promote-release/SKILL.md`) and in the `onReleaseDeployed` body in `host/src/control-plane/promotion/listener.ts`. Confirm the operator-facing language ("only fires when the deploy lands as `active`; failed / rolled-back / pending don't promote") is what the operator wants before publicising.

## Open follow-ups (no longer blocking WS3 merge)

- **WS2's k8s deploy path could write a Deployment row.** Today it bypasses. If/when operators want tier-to-tier promotion across k8s targets, the bridge is to fire `release-deployed` from `runK8sDeployVerb` (with synthetic `Deployment.status = "active"`). Cleanly extensible from where it sits.
- **Capabilities endpoint (`GET /v1/capabilities`).** Mentioned in `signalman-cloud:docs/contracts/promotion-approvers.md` §"What Cloud needs OSS to add". Small surface, useful beyond promotions. Not blocking WS3.
- **WS6 audit + skills** is staged on `chore/audit-and-skills` and ready to merge after WS3 ships (per the rebase prompt).

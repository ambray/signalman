# WS1 status — v0.3.0-5 sub-tasks 5 + 6 + 8

**Branch**: `feat/v0.3.0-5-cloud-finish`
**HEAD**: `5cf383d`
**Started from**: `558e0ed` (main tip on 2026-05-13)
**Date**: 2026-05-14

Sub-tasks delivered in this session:
- **Sub-task 5** (cost-guardrails — TTL reaper, per-org budget, pre-flight
  cost estimate). See "Sub-task 5" section below.
- **Sub-task 6** (networking + per-org credentials at rest). See "Sub-task
  6" section below.
- **Sub-task 8** (CLI parity + per-org credential auto-injection +
  consolidated skills). See "Sub-task 8" section below.

Each is exposed via both MCP and CLI surfaces per the workstream brief.

**Remaining for WS1**: Sub-task 7 (Packer-built golden images — VHDX + AMI
+ Azure managed image in lockstep) explicitly deferred to a session with
cloud credentials + Packer tooling. The Loom plugin handlers
(`loom.signalman.cloud_*`) under `plugins/signalman-loom-plugin/` are a
Rust crate — deferred to a Rust-focused session.

---

# Sub-task 5 — cost-guardrails

This sub-task delivers all three controls from design §13.5 (cost guardrails)
— TTL reaper, per-org budget gate, pre-flight stack cost estimate — each
exposed via both MCP and CLI surfaces.

## Commits

```
21daa61 chore: ignore .commit-msg-temp.txt + remove stray file
c005b97 feat(v0.3.0-5): pre-flight stack cost estimate (sub-task 5 commit 3)
195a399 feat(v0.3.0-5): per-org budget gate + cost table (sub-task 5 commit 2)
dfd6d53 feat(v0.3.0-5): TTL reaper for cost guardrails (sub-task 5 commit 1)
c7bdaa5 docs(workstreams): parallel-plan + 5 starting prompts
```

`c7bdaa5` is the planning artifact; the three feature commits are the
sub-task 5 deliverable. `21daa61` is a stray-file cleanup.

## Tests added

| File | Layer(s) | Tests |
|---|---|---|
| `host/src/__tests__/cloud-reaper.test.ts` | unit + integration + system | 13 |
| `host/src/__tests__/server-reaper-tools.test.ts` | unit (envelope contract) | 3 |
| `host/src/__tests__/cli-cloud-reaper.test.ts` | system | 6 |
| `host/src/__tests__/cloud-cost.test.ts` | unit | 14 |
| `host/src/__tests__/cloud-budget.test.ts` | unit + integration + system | 14 |
| `host/src/__tests__/cli-cloud-budget.test.ts` | system | 5 |
| `host/src/__tests__/cloud-plan-cost.test.ts` | unit + integration + system | 15 |
| **Total** | | **70** |

Plus one update to `cloud-aws.test.ts` (existing assertion adjusted for the new
optional `tags?` field on `CloudInstanceHandle`).

Test taxonomy ratio (per Definition of Done in PLAN.md):
- **Unit**: 47 tests (parser logic, time math, validation, classifiers, argv composition)
- **Integration**: 13 tests (multi-module wiring with one boundary stubbed)
- **System**: 10 tests (full-stack CLI flows, MCP envelope contracts)

## Coverage

| Metric | Before sub-task 5 | After commit 3 | Threshold |
|---|---|---|---|
| Lines | 86.70% | 86.17% | ≥80% |
| Branches | 81.84% | 81.69% | ≥70% |
| Functions | 91.27% | 90.58% | ≥80% |
| Statements | 86.70% | 86.17% | ≥80% |

All four metrics hold well above the gates. Slight dip from baseline because
new code (cost table, migration SQL, repo impls, MCP tool blocks) adds raw
lines that are partially covered (e.g. SKU table entries each count as
covered lines but only a subset is exercised by tests). Real-product code
paths (reaper, budget gate, plan-cost parser, CLI handlers) are all >85%
covered individually.

## 4-lens audit

### QA — PASS

- 70 new tests across 3 layers; all pass on full host suite (1865 total)
- Each commit gated on `npm test` + `tsc --noEmit` + `npm run coverage` before landing
- One flake noted: `kd-lifecycle.test.ts > spawnKernelDebugSessions — disabled path` timed out at 5s on one run, passed on retry and when run in isolation. Pre-existing flaky test under parallel-worker load — not a regression from this sub-task
- Existing 1795 tests unchanged in behaviour; the one mutation (`cloud-aws.test.ts:510` deep-equal → `toMatchObject`) was a forced consequence of adding the optional `tags?` field to `CloudInstanceHandle` and is documented in commit 1's message

### Architecture — PASS

- **Reaper**: backend-agnostic via tag inspection on `CloudInstanceHandle.tags`; no per-vendor coupling in `reaper.ts`. The handle's new `tags?` field is **optional** + **informational** so back-compat is preserved (no caller breaks; existing handles with no tag = "no TTL" = skipped)
- **Budget gate**: clean storage / business-logic separation. `CloudBudgetGate` takes injectable `CloudBudgetRepo` + `CloudUsageRepo` — tests substitute in-memory SQLite; production wires through `ControlPlane.cloudBudgets / cloudUsage`
- **Module-level singletons** (`getOrCreateReaper`, `setBudgetGate / getBudgetGate`) — chosen over constructor injection on AWS/Azure backends because (a) the registry pattern requires no-arg factories, (b) tests can wire/clear the singleton per case via `reset*ForTests` helpers
- **Cost table is a static const + lookup function**; no DI, no API call, no network dependency on the hot path. Design §13.5 acknowledges this as "naive at first"; real pricing API is documented v0.3.x followup
- **Migration block 0040–0049** reserved for this sub-task per `docs/workstreams/PLAN.md`. Used `0040_cloud_budgets.sql` only; 0041–0049 free for sub-tasks 6–8

### Product — PASS

- Both MCP and CLI surfaces shipped per the user's "expose new capabilities via MCP and CLI" directive
- MCP tools have natural-language descriptions agents can reason over (trigger phrases for "force a reaper sweep", "show me the org's budget", "what would this stack cost")
- CLI verbs use `--format json` for scripting and human-readable output by default; exit codes mapped to existing project conventions (0 success, 4 backend error)
- Operator-facing guardrails baked into commit messages + module headers: idempotent terminate / destroy, absent-tag = no-TTL = skip (back-compat), unknown-SKU fallback rate is high (over-protect not under-protect)
- "Naive estimate" is honest in operator-facing output: CLI footer reads "Estimates use a static SKU × region table from v0.3.0-5; treat as guardrail, not invoice."
- SKILL.md updates batched for a followup commit (per the sub-task 8 plan in PLAN.md) — operators can already use the verbs/tools directly; agent-discovery via skill files lands with the rest of the sub-task 8 surface

### Security — PASS

- **Sentinel-tag spoofing defence** carried forward: caller `tags` cannot override `signalman-managed`, `signalman-org`, `signalman-ttl-minutes`, or the new `signalman-ttl-expires-at`. AWS + Azure backends filter them in `buildInstanceTags` / `buildAzureTags`
- **Reaper malformed-tag guardrail**: a corrupt `signalman-ttl-expires-at` value (non-numeric, negative, NaN) is treated like absent — skipped + logged with structured stderr. Explicit protection against a single bad tag triggering a sweep-mass-terminate. Test coverage exists for all 4 corruption shapes
- **Budget gate runs before any vendor API call**: `gate.checkForConfig(config)` throws on over-budget BEFORE EC2 / Azure SDK invocation. No way for an over-budget caller to land an instance even momentarily
- **Storage layer uses parametrised queries throughout**: both SQLite and Postgres repos use `?` / `$N` placeholders, never string concatenation, for org_id and instance_id. No SQL injection surface
- **Best-effort usage records** intentionally swallow storage errors after successful provision — but **only after** the budget check has passed. Failing to record usage cannot result in over-budget provision (because the check happens first)
- **Cost table is read-only** (`Object.freeze`); operator-side runtime mutation impossible
- **No credentials handled** in this sub-task — all credential code paths already existed in sub-tasks 2/3 and remain unchanged
- **CLI commands honour `SIGNALMAN_DATA_DIR`** for storage isolation (tests use this for per-case sandboxing); no hardcoded paths

## Deferred (documented; tracked for sub-task 6/7/8)

- **CLI verbs for the v0.3.0-5 sub-task 4 MCP tools** (`signalman cloud provision`, `terminate`, `status`, `list`, `backends`; `signalman stack apply`, `destroy`) — explicit sub-task 8 scope. Operators that want these today drive them via MCP from a Claude Code session
- **Loom plugin handlers** for the new tools (`loom.signalman.cloud_*` / `loom.signalman.budget_*`) — sub-task 8 followup
- **SKILL.md updates** pointing agents at `signalman_reaper_*`, `signalman_budget_*`, `signalman_stack_plan_cost` — batched with sub-task 8 skill commit
- **Vendor real-pricing APIs** (AWS Price List, Azure RateCard) — v0.3.x precision-pricing pass; design §13.5 explicitly accepts "naive at first" for v0.3.0-5
- **Daemon-mode reaper** (always-on scheduler integrated with `signalman serve`) — sub-task 6/7 wires the scheduler into the long-running daemon; today operators run via cron / systemd / one-shot `signalman cloud reaper run`
- **Audit log integration** for budget events (warned / refused / set) — best-effort structured stderr logs only; sub-task 6/7 wires `POST /v1/audit` emitters
- **Plan-cost interactive confirmation** (`signalman stack apply --plan-cost-first` prompting "Continue? [y/N]") — sub-task 8 ties the verbs together with a confirmation flow
- **Per-attribute cost extraction** (gp3 disk type, IOPS, cross-region data transfer) — v0.3.x; covers the long tail of resources that aren't compute SKUs

## Operator review needed

- **None blocking consolidation**. Sub-task 5's three controls are complete, tested, gated, and audited. The `.commit-msg-temp.txt` leak in commit `c005b97` was caught immediately and cleaned up in `21daa61` — no remediation required, but operator may want to confirm the cleanup commit is the kind of mistake-acknowledgement they want in history vs. a force-push reorder. Project CLAUDE.md says "Prefer to create a new commit rather than amending" — followed that convention.
- **Coverage trended down slightly** (86.70 → 86.17 lines) — well above threshold, but for v0.3.0-6+ planning, note that adding more raw lines of code (SKU tables, CLI verb blocks, MCP tool blocks) without proportional tests is the slow leak. Worth a meta-test that excludes tabular data (cost.ts SKU table) from the coverage denominator if the trend continues.
- **One flaky pre-existing test** (`kd-lifecycle.test.ts > spawnKernelDebugSessions — disabled path`) — not regressed by this sub-task; consider a vitest `retry` annotation on that case in a followup.

## Consolidation flow (per PLAN.md)

```bash
# From the main signalman repo (not the worktree)
cd ~/source/repos/signalman
git fetch origin
git checkout main
git pull --ff-only origin main
git merge --ff-only feat/v0.3.0-5-cloud-finish
git push origin main

# Then clean up the worktree
git worktree remove ../signalman-cloud-finish
git branch -d feat/v0.3.0-5-cloud-finish
```

If the operator wants the planning artifact (`docs/workstreams/PLAN.md` etc)
to land on `main` independently of sub-task 5 (so the other workstreams'
agents can reference it without pulling the cloud-finish code), cherry-pick
`c7bdaa5` first.

---

# Sub-task 6 — networking + per-org credentials

## Commits (sub-task 6)

```
776258e feat(v0.3.0-5): per-org credentials at rest (sub-task 6 commit 2)
a8c15cb feat(v0.3.0-5): network modes for guest reachability (sub-task 6 commit 1)
```

## Surface delivered

Per design §13.6 (networking modes for guest reachability) and §13.7
(layered credential model — at-rest encryption layer).

**Network modes** — three values on `CloudInstanceConfig.network.mode`:

- `public_mtls` (default; back-compat): vendor assigns public IP,
  security group restricts inbound, mutual TLS authenticates the
  control-plane caller
- `aws_ssm`: AWS-only; forces `AssociatePublicIpAddress=false` regardless
  of `assign_public_ip`; control plane reaches the guest via SSM Session
  Manager port forwarding (tunneling implementation is a v0.3.x follow-up;
  this commit ships the contract)
- `azure_bastion`: Azure-only; records mode on handle; control plane
  reaches the guest via Bastion native client port forwarding

Cross-vendor mode passes raise `invalid_config` at provision time
(`aws_ssm` on Azure backend; `azure_bastion` on AWS backend).

The provisioned handle records `network_mode` so any control-plane recovery
path (re-attach to a running VM) sees the right mode without re-asking the
operator.

**Connection descriptors** (`host/src/cloud/connection.ts`):

`getConnectionDescriptor(handle, opts)` returns a tagged union with the
addressing parameters a caller needs to dial the guest:

- `public_mtls`: `{ kind, port, host? }`
- `aws_ssm`: `{ kind, region, instance_id, port }`
- `azure_bastion`: `{ kind, subscription_id, resource_group, vm_name, port }`

`withResolvedHost(descriptor, ip)` populates the public-IP host field after
the caller resolves it via `backend.getInstanceIp(handle)`.

**Per-org credentials at rest** (`host/src/cloud/credentials.ts`):

- AES-256-GCM with a 32-byte key from `SIGNALMAN_CRED_KEY` env var
  (base64-encoded). Fail-loud on missing/malformed key.
- Per-row random 12-byte IV; 16-byte auth tag appended to ciphertext.
  On-disk layout: `base64(iv || ciphertext || auth_tag)`.
- AWS plaintext: `{ access_key_id, secret_access_key, session_token? }`.
- Azure plaintext: `{ tenant_id, client_id, client_secret }`.
- Redaction hint computed at encrypt time (`AKIA****MPLE` for AWS, partial
  client_id for Azure). Stored alongside ciphertext; surfaced via read
  paths so operators can confirm "yes, right key" without exposing the
  secret.
- `loadCredentialForOrg(repo, orgId, backend)` returns decrypted plaintext
  or null. Unknown `encryption_method` on stored row rejected with
  `invalid_config` so a future KMS-rotation pass can distinguish without
  ambiguity.

**Storage**: migration `0041_cloud_credentials.sql` adds
`cloud_org_credential (id PK, org_id, backend, ciphertext_b64,
encryption_method, redacted_hint, created_at, updated_at)` with unique
index on `(org_id, backend)`. SQLite + Postgres repos.

## Operator surface — MCP + CLI parity per the workstream brief

| MCP tool | CLI verb |
|---|---|
| `signalman_cloud_connection_descriptor` | `signalman cloud connection-descriptor` |
| `signalman_creds_set` | `signalman cloud creds set` |
| `signalman_creds_get` | `signalman cloud creds get` |
| `signalman_creds_remove` | `signalman cloud creds remove` |

Plus the network-mode field was added to the existing `signalman_cloud_
provision` MCP schema.

## Tests added (sub-task 6)

| File | Layer(s) | Tests |
|---|---|---|
| `host/src/__tests__/cloud-connection.test.ts` | unit + integration | 15 |
| `host/src/__tests__/cli-cloud-connection.test.ts` | system | 7 |
| `host/src/__tests__/cloud-credentials.test.ts` | unit + integration + system | 20 |
| `host/src/__tests__/cli-cloud-creds.test.ts` | system | 7 |
| `host/src/__tests__/cloud-aws.test.ts` | integration (network-mode tests added) | +4 |
| `host/src/__tests__/cloud-azure.test.ts` | integration (network-mode tests added) | +2 |
| **Total new** | | **55** |

Taxonomy split:
- **Unit**: 32 tests (encryption round-trip, redaction-hint formatter,
  descriptor builder, fail-loud key-loading)
- **Integration**: 13 tests (backend handle propagation, repo + encryption
  pipeline, repo CRUD)
- **System**: 10 tests (CLI dispatch, full operator workflow including
  credential rotation, plaintext-never-leaks verification)

## Coverage (after sub-task 6)

| Metric | After ST5 | After ST6 commit 1 | After ST6 commit 2 | Threshold |
|---|---|---|---|---|
| Lines | 86.17% | 86.30% | 86.16% | ≥80% |
| Branches | 81.69% | 81.84% | 81.95% | ≥70% |
| Functions | 90.58% | 90.60% | 90.29% | ≥80% |
| Statements | 86.17% | 86.30% | 86.16% | ≥80% |

All four metrics hold above thresholds across both commits. Branches
actually improved — the additional dispatch logic (network-mode switch,
backend dispatch) added new branches that are all covered by tests.

## 4-lens audit (sub-task 6)

### QA — PASS

- 55 new tests across 3 layers; all 1920 full-suite tests pass
- Each commit gated on `npm test` + `tsc --noEmit` + `npm run coverage`
  before landing
- One subtle bug caught by tests: the initial `withTestKey` helper used
  `try / finally` with a returned promise; the finally fired
  synchronously before the async body ran. Caught by 3 immediate test
  failures; fixed in the same commit by switching to `async / await`
- The 2 pre-existing `toEqual` handle assertions in cloud-aws and
  cloud-azure tests were relaxed to `toMatchObject` because the new
  `network_mode` field on the handle is intentional surface — documented
  in commit 1's message

### Architecture — PASS

- **Network mode is data-on-handle**, not state-in-registry. Backends
  record the mode on the returned handle; downstream callers
  (`getConnectionDescriptor`) read it without round-tripping to storage.
  Recovery paths (handle JSON round-trip) preserve the mode
- **Connection descriptors are pure data**, no I/O. The actual
  tunneling protocol (SSM session-manager, Bastion port forwarding) is
  caller-side concern — a future control-plane connection driver
  consumes these descriptors and dispatches to the right tunnel impl
- **Credentials use Node's built-in `node:crypto`** — no third-party
  crypto deps, no native compilation. AES-256-GCM is FIPS-grade and
  industry standard
- **Module-level key cache** with `resetEncryptionKeyForTests` helper
  matches the budget-gate / reaper singleton patterns from sub-task 5
- **Loader-vs-storage separation**: `CloudCredentialsRepo` holds
  ciphertext; `loadCredentialForOrg` is the decrypter at the call site;
  the AWS/Azure backends do NOT yet auto-consume the loader (deferred
  per the "deferred" section below)

### Product — PASS

- All three modes are operator-visible from both MCP and CLI; the
  trigger phrases agents would use ("provision a VM with no public IP",
  "tunnel through SSM", "use Azure Bastion") map cleanly to the
  `network.mode` schema field
- Connection-descriptor verb gives operators a debuggable view of "how
  WOULD I reach this VM" without having to dial it
- Credentials surface intentionally narrow: set / get / remove. No
  list-all-orgs (deferred); no plaintext echo on get; redaction hints
  visible in human-readable AND JSON output paths
- Fail-loud on missing `SIGNALMAN_CRED_KEY` — operators get an
  immediate clear error rather than a silent insecure default
- Commit messages explicitly document the deferred auto-injection (set
  creds today via CLI/MCP; provisionInstance still uses the SDK default
  chain — wiring is sub-task 6 commit 3 / sub-task 8 scope)

### Security — PASS

- **AES-256-GCM with per-record random IV.** Nonce reuse is
  catastrophic for GCM; rolling fresh randomness per encrypt is the
  safe default. Tested: same plaintext encrypted twice produces
  different ciphertexts
- **Auth tag verification on decrypt.** GCM's built-in MAC catches
  tampering; tested with a 1-bit ciphertext flip
- **Opaque decryption errors.** A failed decrypt throws
  `"failed to decrypt cloud credential"` regardless of cause. The
  underlying crypto error (which could leak length / structure info) is
  swallowed. Different-key decrypt and tampered-ciphertext decrypt both
  surface the same opaque message
- **Plaintext never leaves the encrypter.** MCP `signalman_creds_get`
  returns ONLY the safe fields — id, orgId, backend, redactedHint,
  encryptionMethod, createdAt, updatedAt. The CLI `creds get` output
  is the same shape. Tests assert that plaintext substrings do NOT
  appear in captured stdout
- **Fail-loud on missing key.** No silent fallback to plaintext storage
  if `SIGNALMAN_CRED_KEY` is unset — operators must opt in. Audit
  reasoned out the alternative (silent fallback) as a security trap
- **Stable redaction hint computed at encrypt time** (not on read).
  Reading "just the hint" never decrypts the secret — minimal exposure
  surface
- **Encryption method versioned.** Stored alongside ciphertext so a
  future KMS-rotation pass can identify rows to migrate; an unknown
  method is rejected at load time
- **Network-mode invariant**: `aws_ssm` forces
  `AssociatePublicIpAddress=false` even if the operator's
  `assign_public_ip` field would otherwise enable it. Tested with an
  explicit `assign_public_ip: true` config to confirm the override
- **Sentinel-tag spoofing defence** from sub-task 5 carries forward
  unchanged

## Deferred (tracked for sub-task 6 commit 3 / sub-task 7 / sub-task 8)

- **Auto-injection of per-org credentials into AwsBackend / AzureBackend
  constructors.** The storage + decryption + CLI/MCP surface is ready;
  the wiring into `provisionInstance` is a separate behavioural change
  (~30 LOC + new `getCloudBackendForOrg(kind, orgId)` helper). Sub-task
  6 commit 3 OR sub-task 8 will land it
- **KMS-derived keys** (`aws-kms`, `azure-key-vault`, `age-encrypted-
  file` per design §13.7). v0.3.x followup; same table, new
  `encryption_method` values
- **`signalman cloud creds rotate-key`** — bulk re-encrypt with new key.
  v0.3.x; needs the dual-key decrypt-old-encrypt-new path
- **Per-target overrides** (`Target.connection.credential_ref`). The
  Target row pathway isn't on the cloud_* path yet; sub-task 8 wiring
- **Per-runtime overrides** (`--aws-profile staging`, `--azure-
  credentials path/to/sp.json`). Already work via the AWS/Azure SDK
  default credential chain — Signalman just needs to NOT block them
  (it doesn't). Documented as operator pattern; no Signalman code path
- **Actual SSM Session Manager tunneling client** — the `signalman
  cloud connect <handle>` verb that produces a live forwarded socket.
  v0.3.x; this commit ships the descriptor contract that any tunneling
  driver will consume
- **Actual Azure Bastion native-client port-forwarding driver** — same
  treatment; descriptor ships, driver is followup
- **Audit log integration** for credential-touched events (encrypt /
  decrypt / rotate). Best-effort structured stderr logs only; sub-task
  6/7 wires `POST /v1/audit` emitters

## Operator review needed

- **None blocking consolidation**. Sub-task 6's two commits deliver
  the storage + abstraction surface for §13.6 and §13.7. The deferred
  items above are all explicit followups, documented in commit
  messages and in this status report
- **Branch coverage trended UP** (81.69 → 81.95) — the new dispatch
  logic (network-mode switch, backend dispatch in CLI) added covered
  branches, which is a healthy sign that the new code paths are
  exercised by tests
- **Functions coverage dipped slightly** (91.27 → 90.29) — the new
  module exports (e.g. unused `_CloudUsageRepoTypeAlias` for type
  symmetry, plus a few internal helpers) add to the denominator
  without corresponding individual tests. Real product-code functions
  are all >95% covered individually
- Consider documenting the operator workflow for the env-var key
  setup (`how to generate SIGNALMAN_CRED_KEY`, where to store it
  securely) in a future SKILL.md update — bundled with the sub-task 8
  skill batch

---

# Sub-task 8 — CLI parity + credential auto-injection + consolidated skills

## Commits (sub-task 8)

```
5cf383d feat(v0.3.0-5): consolidated skills for sub-tasks 5/6/8 (sub-task 8 commit 3)
86de549 feat(v0.3.0-5): per-org credential auto-injection (sub-task 8 commit 2)
322777d feat(v0.3.0-5): CLI wrappers for cloud + stack tools (sub-task 8 commit 1)
```

## Surface delivered

**Commit 1 — CLI wrappers for sub-task 4 MCP tools**: closes the
operator-side parity gap. Every cloud / stack MCP tool now has an
equivalent CLI verb wired through the same registry-resolved backend
path:

| MCP tool | CLI verb |
|---|---|
| `signalman_cloud_provision` | `signalman cloud provision` |
| `signalman_cloud_terminate` | `signalman cloud terminate` |
| `signalman_cloud_status` | `signalman cloud status` |
| `signalman_cloud_list` | `signalman cloud list` |
| `signalman_cloud_backends` | `signalman cloud backends` |
| `signalman_stack_apply` | `signalman stack apply` |
| `signalman_stack_destroy` | `signalman stack destroy` |

Exit-code mapping: 0 success / 3 auth-or-budget / 4 generic backend
/ 64 usage. The `budget_exceeded → exit 3` split lets CI scripts
distinguish "over budget" from "vendor is having a bad day".

**Commit 2 — per-org credential auto-injection**: bridges the
encrypted-at-rest credential storage from sub-task 6 commit 2 into
the AwsBackend / AzureBackend constructor paths. Behaviour:

  - `org_id` absent or `"default"` → registry's default backend
    (env vars + SDK default credential chain). Back-compat preserved
  - `org_id` present + credential row exists → fresh backend
    constructed with the decrypted plaintext as SDK credentials
  - `org_id` present + row exists + decryption fails → propagates
    `invalid_config` (NOT a silent fallback — privilege-escalation
    surprise avoided)

Tested via injectable `defaultBackend` + `buildBackendWithCreds`
callbacks so the resolver unit tests don't pull in the AWS / Azure
SDKs.

**Commit 3 — consolidated skills**: 5 new SKILL.md files for the
new operator surfaces, 5 existing skills refreshed with
cross-references + CLI parity notes, plus a skill-frontmatter
validator test that asserts the YAML shape so a malformed skill
can't ship silently.

**New skills**:
- `signalman-reap-cloud-instances` — drives the reaper
- `signalman-manage-cloud-budget` — drives budget set/get/usage
- `signalman-estimate-stack-cost` — drives plan-cost
- `signalman-manage-cloud-credentials` — drives creds set/get/remove
  with explicit `SIGNALMAN_CRED_KEY` setup section
- `signalman-build-cloud-connection` — drives the connection
  descriptor (SSM / Bastion / public_mtls addressing)

## Tests added (sub-task 8)

| File | Layer(s) | Tests |
|---|---|---|
| `host/src/__tests__/cli-cloud-stack-verbs.test.ts` | system | 17 |
| `host/src/__tests__/cloud-per-org-backend.test.ts` | unit + integration + system | 8 |
| `host/src/__tests__/skills-frontmatter.test.ts` | system (cross-cutting) | 91 |
| **Total new** | | **116** |

Taxonomy split:
- **Unit**: 5 (resolver dispatch logic, decryption-failure propagation)
- **Integration**: 3 (resolver + SQLite + setCredential pipeline)
- **System**: 108 (CLI argv→handler→stdout, skill frontmatter walk)

## Coverage (after sub-task 8)

| Metric | After ST6 | After ST8 commit 1 | After ST8 commit 2 | After ST8 commit 3 | Threshold |
|---|---|---|---|---|---|
| Lines | 86.16% | 86.16% | 85.89% | 85.89% | ≥80% |
| Branches | 81.95% | 81.94% | 81.95% | 81.98% | ≥70% |
| Functions | 90.29% | 90.29% | 90.11% | 90.11% | ≥80% |
| Statements | 86.16% | 86.16% | 85.89% | 85.89% | ≥80% |

All four metrics hold above thresholds. Branches actually IMPROVED
(81.95 → 81.98) thanks to the explicit dispatch logic added by
commit 2 (per-org backend resolution) and commit 3 (skill
frontmatter parser).

## 4-lens audit (sub-task 8)

### QA — PASS

- 116 new tests; all 2036 full-suite tests pass on the head commit
- Each commit gated on `npm test` + `tsc --noEmit` + `npm run
  coverage` before landing
- The skill-frontmatter validator caught a real bug during
  development: the regex didn't tolerate CRLF (Windows
  `core.autocrlf`). 50 tests failed initially with "expected null
  not to be null" until I added the `replace(/\r\n/g, "\n")`
  normalisation. Good defensive harness; would have surfaced
  the same issue on Linux CI later
- No flakes observed this session (the pre-existing kd-lifecycle
  flake from sub-task 5 didn't recur on the larger 2036-test suite)

### Architecture — PASS

- **CLI wrappers are thin pass-throughs.** Every `cmdCloud*` /
  `cmdStack*` handler does argv parsing → option validation →
  backend resolution → MCP-equivalent call → output formatting.
  No business logic in the CLI layer; corrections to provisioning
  / termination logic only need to touch the backend
- **Per-org backend resolver is an HOF.** Production wiring
  injects `defaultBackend: () => getCloudBackend(kind)` and
  `buildBackendWithCreds: defaultBuildBackendWithCreds`; tests
  inject stubs. This separation lets the unit tests live without
  the AWS / Azure SDKs (which would explode the test boot time +
  pull network deps)
- **No registry caching for per-org backends.** The registry's
  "one client per kind" invariant (sub-task 1) holds for the
  env-credential path only. Per-org backends are constructed
  fresh per call; an eventual `Map<(kind, orgId), backend>` cache
  is a v0.3.x followup when real-trace data shows the cost
- **Skill frontmatter validator is a single test file** that
  walks the directory at run-time. Adding / renaming a skill
  doesn't need test-code changes — just an SKILL.md with the
  right frontmatter. The validator catches drift, not greenfield
  additions
- **Documented deferrals are explicit, not implicit.** The Loom
  plugin (Rust crate), per-org Azure scoping, KMS-derived keys,
  the actual SSM / Bastion tunneling clients, and golden images
  are all called out by name in commit messages + this status
  doc; future sessions know exactly what's missing

### Product — PASS

- **MCP-CLI parity is now complete** for the cloud + stack
  surface. The summary table at the end of commit 3 enumerates
  16 MCP tools, 16 CLI verbs, 10 skills — each operator
  capability is reachable from at least 2 of the 3 surfaces
- **The new skills' "What NOT to do" sections** carry the
  guardrails learned from previous sub-tasks forward. The
  creds skill especially: "never paste plaintext in chat",
  "never disable SIGNALMAN_CRED_KEY check", "never assume get
  returns the secret"
- **The skill-frontmatter validator** also implicitly raises the
  bar for trigger-phrase quality — the heuristic test for
  "description has at least one operator-trigger phrase
  indicator" catches abstract skills that the agent runtime
  would silently skip
- **Capability-matrix table** in commit 3's message gives
  operators a single-source view of "what can I do with the
  cloud surface today, via which entry point". A future
  capability-audit pass (WS6) consumes this verbatim
- **Per-org credential auto-injection is silent on the
  happy-path.** Operators set creds via `signalman cloud creds
  set`, then call `signalman cloud provision --org-id acme`,
  and the credentials Just Work. The CLI / MCP surface doesn't
  surface the encryption-decryption dance; only the failure
  cases (missing key, decryption failure) bubble up

### Security — PASS

- **Plaintext credentials still NEVER cross the MCP / CLI
  boundary.** Sub-task 6 commit 2's guarantee carries forward;
  sub-task 8 commit 2 only adds the *internal* decryption-at-
  call-site path. The MCP `signalman_creds_get` and CLI
  `signalman cloud creds get` still return only the redacted
  hint. The new commit 2 path decrypts INSIDE the resolver,
  passes the plaintext into the SDK client constructor, and
  immediately discards the variable reference
- **Decryption failure propagates as `invalid_config`.** Critical
  invariant: when a credential row exists but the key is wrong
  / ciphertext is corrupt, the resolver throws. Silent fallback
  to the SDK default chain would be a privilege-escalation
  surprise (operator expected per-org creds; got their env-var
  ones instead). Tested explicitly
- **No new SQL paths** — sub-task 8 reuses the credentials repo
  from sub-task 6 with no schema changes
- **Skill frontmatter validator catches one security-relevant
  drift**: a malformed `allowed-tools` field would let a skill
  silently lose its scoping and gain access to all tools.
  Validator asserts the field is non-empty
- **Per-org backend construction is per-call (uncached).** A
  side benefit: if an operator rotates credentials between
  provision calls, the second call sees the new credentials
  without any cache invalidation step
- **CLI verbs honour exit-code conventions** so wrapping scripts
  can reliably distinguish failure types. `budget_exceeded → 3`
  vs `terminate_failed → 4` is a security-adjacent distinction:
  budget exhaustion is a policy decision, not an outage
- **Sub-task 4's existing sentinel-tag spoofing defence** carries
  forward unchanged

## Deferred (tracked for sub-task 7, sub-task 8 followups, v0.3.x)

- **Sub-task 7**: Packer-built golden images (VHDX + AMI per region
  + Azure managed image per region in lockstep). Requires Packer
  binary + AWS + Azure credentials in a CI lane; deferred to a
  session with that toolchain
- **Loom plugin handlers** (`plugins/signalman-loom-plugin/` —
  Rust crate). Different toolchain; deferred to a Rust-focused
  session
- **Per-org Azure scoping**: today the credential plaintext
  identifies the SP; subscription / resource group / region
  still come from env vars. v0.3.x can add a scoping table
  alongside `cloud_org_credential`
- **Per-org backend cache**: `Map<(kind, orgId), backend>` if
  real-trace data shows the per-call construction cost. v0.3.x
- **Wiring `resolveBackendForRequest` into the rest of the
  cloud MCP tools** (terminate, status, list). Today only
  `signalman_cloud_provision` uses the per-org path; the other
  three still go through `getCloudBackend(kind)`. Sub-task 8
  followup OR v0.3.x — depends on how operators end up using
  the per-org creds
- **KMS-derived keys** (aws-kms, azure-key-vault, age-encrypted-
  file per design §13.7). v0.3.x
- **`signalman cloud creds rotate-key`**: bulk re-encrypt with
  new key. v0.3.x
- **Actual SSM Session Manager + Azure Bastion tunneling
  clients**: descriptor contract shipped in sub-task 6 commit 1;
  drivers are v0.3.x
- **Wiring the cost-budget gate into `release deploy --target
  cloud_stack_*`** (per design §13.5 control 3 second half).
  Today plan-cost is operator-invoked; auto-prompting before
  apply is a sub-task 7 or sub-task 8 followup

## Operator review needed (sub-task 8)

- **None blocking consolidation.** Sub-tasks 5, 6, and 8 close
  out the cost-guardrails (§13.5), networking (§13.6), and
  layered-credential (§13.7) design sections, plus the deferred
  CLI parity from sub-task 4
- **Sub-task 7 still required for v0.3.0-5 completeness.** Golden
  images are an explicit design deliverable. The current branch
  can ship for any operator who's content with their own AMI /
  Azure-image curation; the lockstep manifest workflow is
  followup
- **Loom plugin still pending.** Operators driving cloud
  operations via Loom workflows can't today — only the MCP
  surface (Claude Code) and CLI (shell / CI) routes work. The
  Rust crate skeleton is in `plugins/signalman-loom-plugin/`
- **Branch now has 8 feature commits + 4 docs/cleanup commits**
  ahead of `main` (12 total). Consolidation flow at the bottom
  of this doc is unchanged

## Final summary table (full sub-task 5/6/8 surface)

**16 MCP tools / 16 CLI verbs / 10 skills, all consistent across
MCP and CLI surfaces**:

| Capability | MCP tool | CLI verb | Skill |
|---|---|---|---|
| Provision VM | signalman_cloud_provision | signalman cloud provision | signalman-provision-cloud-vm |
| Terminate VM | signalman_cloud_terminate | signalman cloud terminate | signalman-terminate-cloud-vm |
| VM status | signalman_cloud_status | signalman cloud status | (provision skill) |
| List VMs | signalman_cloud_list | signalman cloud list | signalman-list-cloud-instances |
| List backends | signalman_cloud_backends | signalman cloud backends | (provision skill) |
| Apply stack | signalman_stack_apply | signalman stack apply | signalman-apply-cloud-stack |
| Destroy stack | signalman_stack_destroy | signalman stack destroy | signalman-destroy-cloud-stack |
| Plan-cost | signalman_stack_plan_cost | signalman stack plan-cost | signalman-estimate-stack-cost |
| Reap past-TTL | signalman_reaper_run_once | signalman cloud reaper run | signalman-reap-cloud-instances |
| Reaper status | signalman_reaper_status | signalman cloud reaper status | (reap skill) |
| Budget get/set/usage | signalman_budget_{get,set,usage} | signalman cloud budget {get,set,usage} | signalman-manage-cloud-budget |
| Creds set/get/remove | signalman_creds_{set,get,remove} | signalman cloud creds {set,get,remove} | signalman-manage-cloud-credentials |
| Connection descriptor | signalman_cloud_connection_descriptor | signalman cloud connection-descriptor | signalman-build-cloud-connection |

---

# WS6 milestone 5 — audit log surface (post-consolidation, 2026-05-15)

After Wave A + Wave B merges landed (WS1–WS5 consolidated; WS6 squash-merged
as `049747e`), this milestone closes the last P2 gap from the Wave-2
capability matrix audit (`docs/audit/capability-matrix-2026-05-wave2.md`):
the audit log was HTTP-only since v0.2.0 (`GET /v1/audit` /
`POST /v1/audit`). M5 ships CLI + MCP parity without changing the
underlying storage shape — the executor-driven append paths (build /
deploy / target edit / runner deregister) are unchanged.

## Surface delivered

| Verb | MCP tool | CLI verb | Skill |
|---|---|---|---|
| Query audit log | `signalman_audit_query` | `signalman audit query` | `signalman-query-audit-log` |
| Append audit entry | `signalman_audit_append` | `signalman audit append` | (same skill, dual-verb) |

Query filters AND-combine: `entity_type`, `entity_id`, `actor`, `action`,
`since` (ISO-8601 lower bound on `created_at`), `limit`. Repo handles
`entity_type` + `entity_id` + `limit` natively; the rest are applied at
the verb layer.

Append validates all four entity fields non-empty (`actor`, `action`,
`entity_type`, `entity_id`); `detail` is optional and stored as a
free-form JSON object. The log remains immutable — no update / delete
verb is exposed.

## Tests added (M5)

| File | Layer(s) | Tests |
|---|---|---|
| `host/src/__tests__/audit-verbs.test.ts` | unit + integration | 15 |
| `host/src/__tests__/skills-frontmatter.test.ts` | system (existing harness picks up new skill) | +1 skill (242 → wider scope) |
| **Total new** | | **15** |

The frontmatter validator cross-checks `allowed-tools` against the
`server.tool(...)` registrations in `server.ts` — confirms both
`signalman_audit_query` and `signalman_audit_append` are wired.

HTTP integration coverage of `/v1/audit` already exists in
`http-app.test.ts` and is unaffected (the verbs are a separate path
that lands in the same repo).

## Coverage (after M5)

| Metric | Value | Threshold |
|---|---|---|
| Lines | 84.36% | ≥80% |
| Branches | 82.37% | ≥70% |
| Functions | 88.21% | ≥80% |
| Statements | 84.36% | ≥80% |

136 test files / 2608 passed / 3 skipped. `tsc --noEmit` clean.

## 4-lens audit (M5)

### QA — PASS
- 15 new unit/integration tests; full suite 2608-pass + 3-skip
- Frontmatter validator extended automatically (directory walk picks up
  new skill); no test-code changes required for the additional skill
- Verbs reuse the existing `controlPlane.auditLog.{append,listForOrg}`
  repo methods — no new storage paths exercised, just verb-layer
  filter logic

### Architecture — PASS
- Verb layer is a thin filter wrapper over the existing repo surface;
  no migration, no schema change, no new index
- `actor` + `action` + `since` are post-filtered in the verb (the repo
  doesn't index them); this is acceptable for current cardinality but
  documented in the verb's JSDoc — high-volume queries should narrow
  by `entity_type` first
- MCP tool returns the entry shape exactly as the repo produces it
  (snake_case field rename only); CLI mirrors it. No drift surface
- Skill file follows the same dual-verb convention as
  `signalman-manage-cloud-credentials` (one skill covering
  set/get/remove); avoids the operator confusion of "which skill
  reads vs writes the audit log"

### Product — PASS
- The five operator triggers from Loom (what is active / who owns it /
  what is blocked / what needs approval / what should happen next)
  now have a forensic answer surface via CLI as well as the existing
  HTTP one
- Skill description includes intent triggers ("what happened to
  deployment X", "audit entries since yesterday", "record a manual
  restart") matching the natural-language pattern the agent runtime
  cues on
- "When to use which verb" table at the top of the skill lets agents
  pick query vs append without re-reading the whole file
- Immutability is communicated up-front — the skill explicitly tells
  operators that a typo cannot be edited and offers the corrective-
  append pattern instead

### Security — PASS
- Append refuses empty-string `actor` / `action` / `entity_type` /
  `entity_id` — prevents anonymous / unattributed entries that
  degrade the log's forensic value
- Query is read-only and scoped to the active org via
  `getActiveOrgId(controlPlane)`; no cross-org leakage path
- No new auth surface — the verbs run with the same auth context as
  every other control-plane verb (env-driven local-mode today;
  per-org-key when the host runs in hosted mode)
- Detail field is stored verbatim (`Record<string, unknown>`); the
  skill warns operators not to put secrets there. Considered but
  rejected: a redaction filter — would silently drop fields the
  operator deliberately included, which is worse than relying on
  operator discipline + the skill's warning

## Deferred (tracked)

- **Native indexes on actor / action / created_at** — low priority;
  audit-log table is append-only and grows slowly. Adding indexes
  would speed up the post-filter cases but they're not on a hot path
- **Pagination beyond the `limit` parameter** — today the repo caps
  at 1000 entries via the verb's `z.number().max(1000)`. A
  `before_id` cursor for true pagination is v0.4.x; the use cases so
  far are all "show me the last N for entity X"
- **`signalman audit watch`** — long-running tail à la `kubectl logs
  -f`. The audit log isn't an event bus; operators wanting real-time
  visibility should use the recording / events subsystem instead.
  Skill explicitly warns against polling-as-watch
- **Web UI / TUI surface** for browsing the log — out of scope; the
  CLI + MCP routes serve the operator + agent use cases

## Operator review needed

- **None blocking.** M5 closes the last open P2 gap from the wave-2
  capability matrix. Future audit-log work (indexes, pagination,
  watch) is deferred and explicit
- **Wave-2 gap list remaining**: `runner deploy` (multi-transport
  remote bootstrap — needs design doc first) and `cloud_vm` /
  `cloud_stack` target kinds (full cloud-deploy story). Both are
  larger than M5 and were carved off as their own future milestones
  during the WS6 consolidation

---

# WS6 milestone 6 — P0 micro-skills discoverability (2026-05-15)

Closes the two P0 micro-gaps from the wave-2 capability matrix:
`signalman_promotion_approvals` and `signalman_schedule_run_once`
had no agent-trigger surface. Both tools were already documented in
the nearest skill, but `allowed-tools: Bash` listed no MCP tools,
so an agent invoking either skill couldn't call the tool directly.

## Commit

```
f51d9a5 docs(ws6 m6): wire MCP tools into promote-release + schedule-health skills
```

## Surface delivered

| Skill | Was | Now |
|---|---|---|
| `signalman-promote-release` | `allowed-tools: Bash` | All 7 promotion MCP tools listed (`_add`/`_list`/`_remove`/`_approve`/`_reject`/`_approvals`/`_tick`) + a dedicated "Querying pending approvals" section with intent patterns |
| `signalman-schedule-health` | `allowed-tools: Bash` | 6 schedule MCP tools listed + dedicated "Run one tick manually" section explaining the two-mode operator pattern (daemon vs external cron) |

No code changes — both MCP tools and CLI verbs were already
registered. This was pure discoverability.

## 4-lens audit (M6)

### QA — PASS
- Frontmatter validator (the WS6 enforcement harness) picked up the
  changes automatically; 242 → 254 tests passed cumulative.
- No regressions across the full suite.

### Architecture — PASS
- Pattern matches the audit-log skill's "one skill covers a
  cohesive verb-family" convention.
- Skills declare exact MCP tool names; the validator cross-checks
  against `server.tool(...)` registrations so a drift surfaces at
  test time.

### Product — PASS
- Trigger phrases in descriptions match operator intent ("what's
  pending right now", "fire the scheduler now").
- "When to use which verb" pattern lifted from the audit-log skill
  for consistency across the agent surface.

### Security — PASS
- No new attack surface (no code change).
- The promotion approver allow-list (honour-system) carries
  forward unchanged.

---

# WS6 milestone 7 — WS3 promotion auto-approver health-gate (2026-05-15)

Closes the wave-2 WS3↔WS2 integration gap. A tier-to-tier promotion
policy can declare a `health_gate` in its `gate_config` that defers
auto-promotion until the source-tier deployment has accrued N
consecutive recent health-check passes inside a configurable window.
Works across all target kinds (vm, k8s, cloud_*) because it reads
from the existing `health_check` table — no k8s-specific code path.

## Commit

```
a7c3b84 feat(ws6 m7): WS3 promotion auto-approver health-gate (WS2 readiness)
```

## Schema

Migration `0071_promotion_health_gate.sql`:

```sql
ALTER TABLE approval ADD COLUMN requires_health_gate INTEGER NOT NULL DEFAULT 0;
CREATE INDEX approval_health_gate_idx
  ON approval (requires_health_gate, status)
  WHERE deleted_at IS NULL AND requires_health_gate = 1 AND status = 'pending';
```

Default 0 preserves back-compat. Partial index keeps tick-time scan
cheap regardless of post-deploy approval accumulation.

## Config shape

```jsonc
{
  "gate_config": {
    "health_gate": {
      "min_pass_count": 3,    // last N most-recent checks all 'pass'
      "window_minutes": 30    // newest within this window of now
    }
  }
}
```

## Semantics

| Gate kind | + health_gate | Behaviour |
|---|---|---|
| auto | yes | Listener queues `pending` with `requires_health_gate=1`; tick fires when gate opens. |
| time_delay | yes | AND condition — `auto_approve_at` elapsed AND health gate open. |
| manual | yes | Gate IGNORED. Operator approval is the explicit gate; doesn't need a precondition. |
| any | yes, but `sourceTargetId IS NULL` | Gate IGNORED (initial-tier policy has no source to check). |
| any | no | Original behaviour preserved. |

Operator-driven `signalman_promotion_approve` overrides the gate
unconditionally. A single recent `fail` re-closes the gate (most-
recent N must be uniformly `pass`).

## Tests added (M7)

`host/src/__tests__/promotion-health-gate.test.ts` — 25 tests.

| Category | Count |
|---|---|
| `readHealthGate` (parser) | 7 |
| `isHealthGateOpen` (pure decision) | 6 |
| `firePolicy` integration | 4 |
| `runPromotionTick` integration | 6 |
| Operator approve override | 1 |
| `onReleaseDeployed` integration | 1 |

## Coverage (after M7)

| Metric | After M5 | After M7 |
|---|---|---|
| Lines | 84.36% | (unchanged region; ~84.4%) |
| Branches | 82.37% | improved (new dispatch logic) |

Full suite: 137 files / 2633 passed / 3 skipped after M7.

## 4-lens audit (M7)

### QA — PASS
- 25 new tests; existing 64 promotion/auto-promote tests
  unchanged behaviour.
- Pure functions (`readHealthGate`, `isHealthGateOpen`) tested
  independently; integration tests pin the storage + tick paths.

### Architecture — PASS
- Health gate is data on the `approval` row + parser on the policy's
  `gate_config`; no new repo abstraction.
- Tick path is additive — existing `listPendingAutoApprove` keeps the
  time-delay-only path efficient; new `listPendingHealthGated` adds
  the health-gate path with its own partial index.
- AND condition (time_delay + health_gate) is enforced in the tick,
  not the listener — the listener's job is queueing; the gate logic
  lives in one place.

### Product — PASS
- Operator-facing surface is one new optional field on
  `gate_config`; no breaking changes to existing policies.
- Skill update documents the AND semantic + manual-ignored + operator
  approve override prominently.

### Security — PASS
- Operator approve still overrides; the gate doesn't create a path
  that lets a misconfigured policy lock out manual intervention.
- Stale (`requiresHealthGate` row whose policy lost its
  `health_gate` config) approvals are correctly tolerated — the
  tick re-parses on every pass.

## Deferred (M7)

- **Negative-feedback gates** (e.g. "require 3 fails before halting")
  — only positive-pass gates today. The use case is rare; track if
  operator demand surfaces.
- **Per-probe gating** (e.g. "require only the `http_200` probe to
  pass") — today the gate considers ALL probes in the source
  deployment's health-check history. Refinement is v0.5+ if needed.

---

# WS6 milestone 8 — cloud_vm + cloud_stack target kinds (2026-05-15)

Closes wave-2 gap #4. Adds two new target kinds for cloud-routed
release deploys, each with its own deploy semantic but sharing the
standard Deployment row lifecycle.

## Commit

```
1b2da08 feat(ws6 m8): cloud_vm + cloud_stack target kinds + deploy adapters
```

## Schema

Migrations `0072_target_kind_cloud.sqlite.sql` + `.pg.sql`:

```sql
-- CHECK (kind IN ('vm_test', 'vm_demo', 'docker_test', 'docker_demo',
--                 'k8s_test', 'k8s_demo', 'cloud_vm', 'cloud_stack'))
```

Append-only; existing rows unaffected.

## Surface delivered

| Kind | Connection shape | Deploy semantic |
|---|---|---|
| `cloud_vm` | `{ provider, region, instance_id, name, network_mode?, guest_agent_port? }` | Resolve public IP via cloud backend, TCP-reach probe at guest agent port, record `cloud_vm_reachable` health check. |
| `cloud_stack` | `{ stack_name, module_path, image_var_name?, extra_vars? }` | Invoke `TofuDriver.applyModule` with per-release vars (`release_tag`, `release_id`, `release_commit_sha` + optional `<image_var_name>=<release.tag>`); record `stack_apply` health check. |

Dispatcher: `isCloudTargetKind(kind)` sibling to `isK8sTargetKind`;
`runReleaseDeploy` routes cloud kinds to `runCloudReleaseDeploy`.

## Tests added (M8)

`host/src/__tests__/cloud-deploy.test.ts` — 26 tests across 4 areas:
parsers (10), cloud_vm deploy (7), cloud_stack deploy (5),
dispatcher integration (4).

Coverage held above gates (84.55/82.62/88.32/84.55).

## 4-lens audit (M8)

### QA — PASS
- 26 new tests with mocked cloud backend + TofuDriver; no real AWS /
  Azure / `tofu` invocations.
- Full suite 138 files / 2659 passed / 3 skipped after M8.

### Architecture — PASS
- Cloud adapters mirror the k8s pattern (`runK8sReleaseDeploy`)
  rather than reusing the heavyweight VM-backed `runDeploy`. Each
  target-kind family gets its own thin adapter.
- `runReleaseRollback` explicitly refuses cloud kinds with a clear
  operator pointer — kind-specific rollback warrants its own
  milestone (tracked as carve-out #2 in wave-3).
- `aws_ssm` / `azure_bastion` network modes refused BEFORE creating a
  Deployment row — fail-fast on misconfig.

### Product — PASS
- Connection parsers throw operator-friendly errors with field
  names; no cryptic JSON-validation messages.
- Stack outputs land in audit-log detail — operator can answer
  "what URL did this stack land at" via `signalman release show`.
- `extra_vars` merge cleanly with auto-set release vars; operators
  retain full TF template flexibility.

### Security — PASS
- Caller tags can't override `signalman-managed` etc. (sentinel
  protection from WS1 carries forward).
- Stack rollback path explicitly refuses — no surprise re-applies.
- Sentinel network mode refusal happens before backend call.

## Deferred (M8)

- **cloud_vm install-bundle integration** — today reachability + row
  only; install path is operator-driven (cloud-init / userdata /
  post-deploy hook). Tracked as wave-3 carve-out #1.
- **SSM / Bastion tunneling drivers** — descriptor contract from
  WS1 sub-task 6 in place; live dialers are wave-3 carve-out #5.
- **cloud rollback** — both kinds refuse with operator pointer.
  Tracked as wave-3 carve-out #2.

---

# WS6 milestone 9 — runner deploy multi-transport (2026-05-15)

Closes wave-2 gap #3 (the M3.5 deferral). One verb / one MCP tool /
one CLI subcommand to bootstrap a runner on a remote target via the
right transport for the situation.

## Commit

```
82506a1 feat(ws6 m9): runner deploy multi-transport (script/ssh/winrm/docker/cloud)
```

## Surface delivered

Five transports, one interface:

| Transport | Auth model | Verification |
|---|---|---|
| `script` | None (operator runs the emitted script themselves) | Operator-driven; verification skipped |
| `ssh` | Operator-supplied `IdentityFile` | Heartbeat poll |
| `winrm` | Operator-supplied `username` + `password` (env var, not argv) | Heartbeat poll |
| `docker` | Docker context (operator pre-configures) | Heartbeat poll |
| `cloud` | Cloud creds + inner ssh/winrm creds | Heartbeat poll |

Binary source: any HTTP(S) URL (typically a `@signalman/registry`
blob URL). Optional sha256 verification — auto-extracted from
registry blob URLs.

Verification: heartbeat poll on the runners table for up to
`--wait-timeout-ms` (default 60s). Set 0 to fire-and-forget. All
outcome states audit-logged.

## New module

`host/src/runner/deploy/`:
- `transport.ts` — interface + 5 transport option shapes
- `binary.ts` — `RunnerBinaryRef` + URL/sha256 validation
- `heartbeat-wait.ts` — poll runners table with `freshAfter` cutoff
- `script.ts`, `ssh.ts`, `winrm.ts`, `docker.ts`, `cloud.ts` — impls
- `index.ts` — public exports + `runRunnerDeploy` verb

Total new code: ~1500 LOC + 730 LOC tests.

## Tests added (M9)

`host/src/__tests__/runner-deploy.test.ts` — 45 tests:

| Category | Count |
|---|---|
| Binary ref + sha extraction | 7 |
| Script transport (bash/pwsh emission) | 4 |
| Ssh transport (argv + service managers) | 7 |
| WinRm transport (argv + password-via-env) | 5 |
| Docker transport (pull→rm→run sequence) | 7 |
| Cloud transport (dispatch + IP poll) | 5 |
| Heartbeat-wait | 4 |
| Orchestrator (audit + verification) | 6 |

## Coverage (after M9)

| Metric | Value | Gate |
|---|---|---|
| Lines | 84.55% | ≥80% |
| Branches | 82.62% | ≥70% |
| Functions | 88.32% | ≥80% |
| Statements | 84.55% | ≥80% |

Full suite: **139 files / 2716 passed / 3 skipped**.

## 4-lens audit (M9)

### QA — PASS
- 45 new tests with injectable `exec` / `provision` / `getIp` /
  `sleep`. No real subprocesses; no real network; no real cloud SDK
  calls.
- Argv construction pinned for all 5 transports — future refactors
  can't silently break the contract.

### Architecture — PASS
- `RunnerDeployTransport` interface is narrow (`bootstrap(common,
  opts, exec) → BootstrapResult`); each transport is independently
  testable.
- `CloudTransport` orchestrates provision + dispatches to inner
  `SshTransport` / `WinRmTransport` — composition, not duplication.
- All shell-outs go through the injectable `TransportExec`; no
  hardcoded child_process.spawn calls.
- Verification (`waitForRunnerHeartbeat`) is a separate concern from
  bootstrap; transports return success/failure, the verb decides
  whether to verify.

### Product — PASS
- "Which transport when" table in the skill maps operator intent to
  the right verb in one glance.
- `script` transport returns the script body in `bootstrap.script` —
  operators can pipe it / redirect it / commit it.
- Verification audit-log entries give the operator a clear post-
  deploy answer to "did the runner actually start" without manual
  inspection.
- Failure modes carry actionable error messages (exit-code + first
  line of stderr, distinguishing transport vs verification vs
  registration).

### Security — PASS
- WinRM password passes via env var (`SIGNALMAN_WINRM_PASSWORD`),
  not on argv. Audit-log `detail.username` excludes password.
- SSH identity path is operator-supplied; Signalman never handles
  the key material directly.
- Cloud transport refuses to provision a VM if inner credentials
  are missing — fail-fast prevents orphaned cloud instances.
- Cloud transport surfaces the instance handle in the error message
  on inner-bootstrap failure so the operator can terminate manually
  (or let the reaper handle it).

## Deferred (M9)

- **Live integration tests** for each transport against real
  targets — unit tests pin command shapes; integration is operator-
  driven. Tracked as wave-3 carve-out #3.
- **Binary signature verification** — today only sha256. Signed-
  binary verification using the registry's Ed25519 manifest signing
  is a follow-up.
- **`signalman runner deploy --transport k8s`** — k8s already has its
  own dedicated `runner deploy-k8s` verb; not folded into the
  multi-transport surface to avoid breaking the existing k8s deploy
  contract.

---

# WS6 final status (post-M9, 2026-05-15)

**All five WS6 milestones (M5–M9) shipped and consolidated to `main`.**

Workstreams 1–6 are fully merged; gates green across host + registry
packages. The forward-looking "what's left" list is canonical in
`docs/audit/capability-matrix-2026-05-wave3.md` — that doc carries
the recommended sequence for the 9 carve-outs plus the
out-of-scope items.


# WS10 — OCI Distribution Spec v1.1 facade (v0.5 / 2026-05-17)

**Branch**: `feat/v0.5-registry-oci` (dedicated worktree at
`/home/aaron/repos/signalman-registry-oci` after the M1 collision
with WS11's libvirt-parity worktree).
**HEAD**: `405cca1` (M6 commit; M7 docs commit follows).
**Local-main merges**: 7 `--no-ff` merges (`6aa9e86`, `cfcd089`,
`2bd569b`, `133eb38`, `dbeb109`, `0416a51`, plus M7).
**Pushed to origin**: **NO**.

## Commits (8 expected, all on the branch)

| # | SHA | Subject |
|---|---|---|
| M0 | `9a51816` | docs(v0.5-registry-oci): design doc + open questions |
| M1 | `dfed03f` | feat(v0.5-registry-oci): manifest schema + types |
| M2 | `be151da` | feat(v0.5-registry-oci): blob protocol + chunked upload + reaper |
| M3 | `b9dc4db` | feat(v0.5-registry-oci): manifest protocol + tag table |
| M4 | `4f8d4ad` | feat(v0.5-registry-oci): catalog + tags + bearer challenge |
| M5 | `02c7536` | feat(v0.5-registry-oci): Docker Hub + GHCR + ECR virtual upstreams |
| M6 | `405cca1` | feat(v0.5-registry-oci): cosign-style signing on OCI manifests |
| M7 | (next) | docs(v0.5-registry-oci): conformance suite + README + design closure |

## Open questions resolved

Operator locked the eight decisions on 2026-05-16. Recorded verbatim
in `docs/design/registry-oci.md` §Locked design:

| # | Topic | Decision | Delta from rec? |
|---|---|---|---|
| Q1 | Namespacing | Per-org `/v2/<org>/<repo>/...` | No |
| Q2 | Upstreams | **Docker Hub + GHCR + ECR** on day one | **YES** — recommendation was Docker Hub only; M5 ships three adapters incl. full AWS SigV4 |
| Q3 | Cosign timing | In v0.5.0 | No |
| Q4 | Conformance CI | Gated by `SIGNALMAN_OCI_CONFORMANCE=1` | No |
| Q5 | Auth | Full OCI Bearer challenge flow + `/oci/token` | No |
| Q6 | Manifest DELETE | Allowed; `--oci-disallow-manifest-delete` flag to disable | No |
| Q7 | Multi-arch | Full image-index support | No |
| Q8 | Upload UUID TTL | 24-hour persisted + 5-min reaper sweep | No |

## Tests added per layer

562 tests across 32 files (up from 351 at branch cut; +211 from
WS10 alone). By layer:

| Layer | New cases | Where |
|---|---|---|
| Unit — type guards | ~67 | `oci-paths.test.ts`, `oci-errors.test.ts`, `oci-guards.test.ts`, `oci-content-range.test.ts`, `oci-jwt.test.ts`, `oci-sigv4.test.ts`, `oci-upstream-auth.test.ts`, `oci-cosign.test.ts` (cosignTagFor + parseRepoTagRef) |
| Unit — storage helpers | ~25 | `oci-upload-store.test.ts`, `oci-tag-store.test.ts`, `oci-reaper.test.ts` |
| Migration smoke | ~6 | `oci-migration.test.ts` |
| Composition / mount | ~4 | `oci-mount.test.ts` |
| Integration — HTTP | ~109 | `oci-blobs.test.ts`, `oci-manifests.test.ts`, `oci-auth.test.ts`, `oci-catalog.test.ts`, `oci-virtual.test.ts`, `oci-cosign.test.ts` (round-trip flows) |
| System | deferred | Docker-required end-to-end smoke runs in the conformance lane |
| Conformance | scaffolded | `.github/workflows/oci-conformance.yaml` |
| Smoke (existing) | pass byte-identical | cargo + npm + forensic + signing contract tests unchanged |

## Coverage deltas

`registry/src/oci/` aggregate: **92.64 stmts / 85.89 branch / 96.71
funcs / 92.64 lines** — comfortably clears the operator's "≥80 lines
AND ≥80 branches across registry/src/oci/" gate.

Per-file (every metric ≥80 unless flagged):

| File | Stmts | Branch | Funcs | Lines |
|---|---|---|---|---|
| `auth.ts` | 97.5 | 91.89 | 100 | 97.5 |
| `blobs.ts` | 92.58 | 81.94 | 100 | 92.58 |
| `catalog.ts` | 90.78 | 90.69 | 100 | 90.78 |
| `cosign.ts` | 86.05 | **76.19** | 88.88 | 86.05 |
| `errors.ts` | 100 | 96.15 | 100 | 100 |
| `guards.ts` | 100 | 100 | 100 | 100 |
| `http.ts` | 100 | 80 | 100 | 100 |
| `jwt.ts` | 92.25 | 91.3 | 90 | 92.25 |
| `manifests.ts` | 92.51 | 81.45 | 100 | 92.51 |
| `mount.ts` | 96.7 | 92.5 | 100 | 96.7 |
| `paths.ts` | 100 | 100 | 100 | 100 |
| `reaper.ts` | 94.11 | 92.3 | 100 | 94.11 |
| `sigv4.ts` | 100 | **75** | 100 | 100 |
| `tag-store.ts` | 100 | 100 | 100 | 100 |
| `upload-fs.ts` | 97.95 | 88 | 100 | 97.95 |
| `upload-store.ts` | 100 | 100 | 100 | 100 |
| `virtual.ts` | 75.83 | **63.85** | 90 | 75.83 |

Per-file branch dips on `cosign.ts`, `sigv4.ts`, and `virtual.ts`
are confined to defensive `defaultFetch` wrappers around
`globalThis.fetch` and digest-mismatch paths that the sign path
itself cannot exercise (Ed25519 is deterministic; signed bytes match
by construction). The aggregate across the OCI directory satisfies
the operator-locked gate.

## Conformance results

**Status**: scaffolded, not yet run in this environment. The
workflow at `.github/workflows/oci-conformance.yaml` brings up the
registry on `:5000`, mints an Ed25519 signing key, and runs
`ghcr.io/opencontainers/distribution-spec/conformance:v1.1.0`
against it with `OCI_TEST_PULL`, `OCI_TEST_PUSH`,
`OCI_TEST_CONTENT_DISCOVERY`, and `OCI_TEST_CONTENT_MANAGEMENT`
all enabled.

The lane is gated by the repository variable
`SIGNALMAN_OCI_CONFORMANCE=1`. First nightly run is the v0.5.1
ship gate; per-category pass counts will be recorded in this
section when the first run completes.

**Spec sections deferred at v0.5** (out of scope, may roll in for
v0.6):
- Cross-mount (`POST .../uploads/?mount=<digest>&from=<repo>`) —
  optional optimization. Clients fall back to a regular upload
  when not supported.
- Multi-tag push (`PUT .../<digest>?tag=1.2.3&tag=1.2&tag=1`) —
  operator can sequence individual tag PUTs to achieve the same
  result; not a conformance blocker.
- OCI 1.1 referrers API (`GET /v2/<name>/referrers/<digest>`) —
  storage records `subject` on every manifest already; the route
  can be added without a schema change.

## 4-lens audit — final

### QA: **PASS**
- 562/562 tests pass across 32 files.
- Aggregate `src/oci/` coverage 92.64/85.89/96.71/92.64.
- TypeScript clean (preexisting `src/mcp.ts` TS2589 issue unrelated
  to WS10).
- Preexisting unhandled-error noise in `cargo-publish` + `npm`
  oversized-body tests is unchanged on `main`.

### Architecture: **PASS**
- M2–M6 layer cleanly on the M1 schema + types via the
  `mountOciRoutes` aggregator. Adding M7 docs + the conformance
  workflow needed zero code-side modifications.
- The cargo / npm facade pattern (per-org namespacing, `<kind>_metadata_json`
  column, virtual-upstream `config_json` knobs) is preserved verbatim;
  WS10 added one column (`oci_metadata_json`), two tables
  (`oci_tag`, `pending_blob_uploads`), and the new
  OCI-specific config keys on `VirtualUpstreamConfig`.
- The `UpstreamFetch` type was extended (backwards-compatible —
  `method` + `body` fields optional) so the same fetcher shape works
  for cargo's GET-only path and OCI's mixed GET/POST path.
- The `AuditAction` enum gained `delete` (blob DELETE, manifest
  DELETE, reaper-driven upload cleanup); the migration-0002 SQL
  column carries no CHECK constraint so no schema migration was
  needed for the addition.

### Product: **PASS**
- All seven OCI route surfaces wired (blobs, manifests, catalog,
  tags-list, auth challenge, token endpoint, virtual pull-through).
- All eight operator-locked decisions honoured as-shipped.
- Operator workflow documented in `registry/README.md` §OCI:
  `docker login` → `docker push` → `docker pull` → `cosign sign`
  → `cosign verify` → `curl /v2/_catalog` → audit query.

### Security: **PASS — non-negotiable, verified across all milestones**

| Concern | Mitigation |
|---|---|
| **Untrusted manifest validation** | Strict-validating parsers (`oci/guards.ts`) reject every malformed field before persistence: mediaType allowlist, sha256-only digest, non-negative *safe* integer size, all-string annotations, platform.architecture+os mandatory non-empty. JSON parse errors → spec-canonical MANIFEST_INVALID. Content-Type must equal body.mediaType (anti-spoofing). |
| **Upload-UUID enumeration** | 128-bit random IDs (`crypto.randomBytes(16).toString("hex")`). `UploadFsStore.pathFor` validates the 32-hex shape before the filesystem touch — defends against `..` traversal, `/`, uppercase, short, non-hex. |
| **Chunked-upload DoS** | Per-route `maxBodyBytes` cap (default 5 GiB). 24-hour persisted TTL per Q8, refreshed on every PATCH. Reaper sweeps SQL row + tmp file as a pair on a 5-minute cadence so neither side leaks. Cross-repo upload smuggling rejected at PATCH time (upload session is bound to its initiating repository). |
| **Manifest signature trust path** | OCI manifests pinned byte-identical via `index.putManifest(manifest, rawBytes, provenance)` — the OCI digest IS the sha256 of the literal bytes the operator pushed; any re-serialization would corrupt the digest. PUT-by-digest verifies path reference matches computed digest (DIGEST_INVALID otherwise). Foreign (`urls`) layers exempt from blob existence per spec. |
| **Image-index child scope** | Index manifests' child digests must resolve in the SAME repository — cross-repo references rejected as MANIFEST_BLOB_UNKNOWN. |
| **Cosign signature trust** | Ed25519 only on both sign and verify. Verifier re-hashes the payload blob and refuses on storage corruption. Manifest-digest binding inside the payload prevents signature replay across different manifests. Optional `expectedDockerReference` defends against the same signature being passed off as covering a different repo. |
| **Catalog tenant-leak** | `/v2/_catalog` scoped to `oci/%` storage names — cargo + npm namespaces don't bleed in. Per-tenant catalog scoping is RBAC work, deferred to v0.2.1; documented as a known limitation. |
| **JWT replay** | 1-hour TTL (Docker Distribution default). `exp` + `iat` enforced with 60s clock-skew tolerance. Algorithm allowlist (`EdDSA` only) defends against `none` / `HS256` confusion. Audience + issuer pinning rejects cross-realm tokens. |
| **Upstream digest integrity** | When the upstream advertises `Docker-Content-Digest`, the registry re-computes sha256 of the body and refuses on mismatch (audit `phase=digest_mismatch`). Blob proxy fetcher refuses bytes whose sha256 ≠ requested digest. |
| **SigV4 signing-key derivation** | HMAC chain `date → region → service → aws4_request` per spec. Session tokens are signed-headers-included so STS-issued requests can't be replayed without their amz-security-token. |
| **AWS credential handling** | Read from explicit options first, env vars second. Fail-closed when credentials are absent. Never logged. |
| **SQL parameterization** | Every new SQL statement uses `db.prepare(...)` with bound `?` placeholders. No string interpolation. |
| **Path-traversal-safe last cursor on tags/list** | Validated against the OCI tag grammar before being passed to SQL. |
| **Fallback when no challenge realm is configured** | `/v2/` issues 401 *without* `WWW-Authenticate` so Docker CLI clients understand to fall back to direct `sk_` bearer instead of looping on the missing token endpoint. |

## Manual end-to-end test log

**Status**: not run in this environment (Docker + cosign not
available). Programmatic equivalents pass via the integration test
suite — the conformance lane will provide the docker-binary smoke
on first nightly run.

Planned manual smoke commands (record outcomes on first deployment):

```bash
signalman-registry serve --port 8443 --storage-root /tmp/sm \
  --token-signing-key ~/.signalman/keys/signing.key &

docker login signalman-reg.local:8443       # username = sk_PREFIX_SECRET
docker pull alpine:3.20
docker tag alpine:3.20 signalman-reg.local:8443/acme/alpine:3.20
docker push signalman-reg.local:8443/acme/alpine:3.20
docker pull signalman-reg.local:8443/acme/alpine:3.20

cosign sign --key ~/.signalman/keys/signing.key \
  signalman-reg.local:8443/acme/alpine:3.20
cosign verify --key ~/.signalman/keys/signing.pub \
  signalman-reg.local:8443/acme/alpine:3.20

curl https://signalman-reg.local:8443/v2/_catalog
curl https://signalman-reg.local:8443/v2/acme/alpine/tags/list

docker rmi signalman-reg.local:8443/acme/alpine:3.20
```

## Deferred to v0.6+ (with rationale)

| Item | Why deferred |
|---|---|
| Cross-mount (`POST .../uploads/?mount=<digest>&from=<repo>`) | Optimization. Clients fall back to a regular upload. |
| Multi-tag push (`PUT .../<digest>?tag=...&tag=...`) | Optional spec feature. Operators can sequence single-tag PUTs. |
| OCI 1.1 referrers API (`GET /v2/<name>/referrers/<digest>`) | Storage already records `subject` per manifest; route is a thin addition without schema change. |
| Notation / PKI signing variant | Cosign-only at v0.5 per operator-locked Q3. |
| CLI verbs `signalman-registry oci sign|verify` | Programmatic API is complete (`registry/src/oci/cosign.ts`); CLI wrapper is mechanical. v0.5.1 candidate. |
| Vulnerability scanning on ingest (Trivy / Grype) | v0.1.3 ROADMAP item; depends on the OSV-integration scaffold. |
| Per-tenant catalog scoping (RBAC) | v0.2.1 RBAC milestone. Current `/v2/_catalog` is OCI-namespace-scoped but not org-scoped. |
| Full SigV4 across other AWS services | The ECR adapter's `sigv4.ts` is self-contained at 130 lines; if Signalman needs to talk to S3, IAM, etc. the same primitive can serve. v0.6+ if asked. |
| Streaming PUT path for blobs > 1 GB | ROADMAP §v0.2.0 Operational Hardening. Current path buffers per chunk; acceptable for typical image-layer sizes. |

## Operator review needed

1. **Conformance lane bring-up.** Enable `SIGNALMAN_OCI_CONFORMANCE=1`
   on the GitHub repo variables when ready, or trigger a manual
   `workflow_dispatch` run. First run will populate this status file
   with per-category pass counts.
2. **Push WS10 commits to `origin`.** Local main is ahead by 14
   commits across WS9 (signing-service), WS10 (registry-OCI), and
   WS11 (libvirt-parity) — push when the cohort is ready.
3. **CLI verbs.** Decide whether `signalman-registry oci sign|verify`
   should land in v0.5.1 or wait for v0.6. The programmatic API is
   already complete + tested.
4. **Versioning.** `registry/package.json` still reads `0.0.1`; the
   ROADMAP claims v0.1.x is shipping. Resolve in WS12 (OSS-release-
   readiness) at the version-bump pass.
